server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost/powerup
    username: admin
    password: root

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8099/eureka